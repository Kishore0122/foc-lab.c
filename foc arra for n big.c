#include <stdio.h>
int main() {
  int n,i;
  int arr[100];
  printf("Enter the number of elements \n: ");
  scanf("%d", &n);
    for (int i = 1; i <= n; i++) {
    printf("Enter number %d: ", i);
    scanf("%d", &arr[i]);
  }
    if (arr[0]== arr[i]) {
      arr[0] == arr[i];
    }
  printf("%d is biggest",arr[n]);
  return 0;
}